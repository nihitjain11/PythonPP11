# volume of a box

def volume():
	l = float(raw_input("enter the length: "))
	b = float(raw_input("enter the width: "))
	h = float(raw_input("enter the height: "))
	v = l*b*h
	print "the volume of the box is ", v